import ConfirmationView from '@/components/checkout/ConfirmationView';

export const metadata = { title: 'Order confirmed - Lordyeedni Scents' };

export default function ConfirmationPage() {
  return <ConfirmationView />;
}
